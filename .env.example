# Webhook authentication
WEBHOOK_API_KEY=your-api-key-here

# Dashboard login password
DASHBOARD_PASSWORD=admin123

# Session signing secret (change in production)
SESSION_SECRET=4ssound-secret-change-me

# Email forwarding endpoint
EMAIL_FORWARD_URL=https://emsd-ssss-email-escalation.varadise.cloud/api/webhook?email=yhtsang@emsd.gov.hk,emsdrockmanchan@gmail.com

# Database path (local libSQL file)
DATABASE_PATH=file:data/ssss-sound.db

# Base path for serving the app (used when behind a reverse proxy path prefix)
BASE_PATH=/ssss-escalation

# Server port
PORT=3000
